name: stable-sync

# Keep the integration branches of every maintained series up to date with
# their mainline LTS branch. Two series are maintained in parallel (AGENTS.md
# section 1): 6.12 (production baseline, display works via the drm/msm vram
# carveout) and 6.18 (LTS-tracking work, display unsolved - see
# docs/reports/6.18-display-findings.md). Clean merges are pushed directly; a conflict
# opens an issue and leaves the branch untouched (resolve it manually on
# a topic/integration round). See AGENTS.md §5.

on:
  schedule:
    - cron: '17 4 * * *'   # daily, 04:17 UTC
  workflow_dispatch:

permissions:
  contents: write
  issues: write

concurrency:
  group: stable-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1        # one branch at a time, never in parallel
      matrix:
        include:
          - branch: '6.12/baseline'
            stable: 'linux-6.12.y'
          - branch: '6.12/staging'
            stable: 'linux-6.12.y'
          - branch: '6.12/rc'
            stable: 'linux-6.12.y'
          - branch: '6.18/baseline'
            stable: 'linux-6.18.y'
          - branch: '6.18/staging'
            stable: 'linux-6.18.y'
          - branch: '6.18/rc'
            stable: 'linux-6.18.y'
    steps:
      - name: Checkout ${{ matrix.branch }}
        uses: actions/checkout@v4
        with:
          ref: ${{ matrix.branch }}
          fetch-depth: 0
          filter: blob:none

      - name: Fetch stable ${{ matrix.stable }}
        run: |
          git remote add stable https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git
          git fetch --no-tags stable ${{ matrix.stable }}

      - name: Merge stable into ${{ matrix.branch }}
        id: merge
        run: |
          git config user.name  "stable-sync bot"
          git config user.email "stable-sync@users.noreply.github.com"
          BEFORE=$(git rev-parse HEAD)
          STABLE=$(git rev-parse FETCH_HEAD)
          STABLE_DESC=$(git describe --tags "$STABLE" 2>/dev/null || echo "$STABLE")
          echo "stable_desc=$STABLE_DESC" >> "$GITHUB_OUTPUT"
          if git merge-base --is-ancestor "$STABLE" HEAD; then
            echo "result=up-to-date" >> "$GITHUB_OUTPUT"
            echo "Already contains $STABLE_DESC — nothing to do."
            exit 0
          fi
          if git merge --no-edit -m "Merge stable $STABLE_DESC into ${{ matrix.branch }}" FETCH_HEAD; then
            echo "result=merged" >> "$GITHUB_OUTPUT"
            git log --oneline "$BEFORE..HEAD" | head -20
          else
            git merge --abort
            echo "result=conflict" >> "$GITHUB_OUTPUT"
          fi

      - name: Push
        if: steps.merge.outputs.result == 'merged'
        run: git push origin HEAD:${{ matrix.branch }}

      - name: Open conflict issue
        if: steps.merge.outputs.result == 'conflict'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TITLE="stable-sync: conflict merging ${{ steps.merge.outputs.stable_desc }} into ${{ matrix.branch }}"
          if gh issue list --state open --search "$TITLE" --json title -q '.[].title' | grep -qF "$TITLE"; then
            echo "Issue already open."
          else
            gh issue create --title "$TITLE" --body \
              "Automated merge of stable \`${{ matrix.stable }}\` (${{ steps.merge.outputs.stable_desc }}) into \`${{ matrix.branch }}\` hit conflicts and was aborted. Resolve manually per AGENTS.md §5, then re-run the workflow."
          fi
